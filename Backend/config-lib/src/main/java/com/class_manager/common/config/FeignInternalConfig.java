package com.class_manager.common.config;

import org.springframework.context.annotation.Configuration;

/**
 * Feign configuration for internal service calls.
 * This configuration is used to handle internal service-to-service communication.
 */
@Configuration
public class FeignInternalConfig {
    // Configuration for Feign clients used in internal service communication
    // This is a placeholder configuration class
}
