server:
  port: 9090
spring:
  application:
    name: user-auth-service
  datasource:
    url: jdbc:postgresql://localhost:5432/user_auth_db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: "lamyae.himidi@uit.ac.ma"
    password: "hkru jykb hkgq ypuq"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

